#Este archivo va a definir como dbt se va a conectar a nuestra base de datos
default:
  target: dev
  outputs: #Aqui configuramos los entornos de salida que vamos a tener
    dev: 
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      dbname: airflow
      schema: driven_raw
      port: 5432
      